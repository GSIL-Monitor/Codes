<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tsb.dao.read.user.UserSettingReadDao">
	<resultMap id="usResult" type="UserSetting">
	</resultMap>
	
	<select id="get" resultMap="usResult">
		select *  from user_setting where userId = #{userId}
	</select>
	
</mapper>