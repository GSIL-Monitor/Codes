<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tsb.dao.read.CompanyAliasReadDao">
	<resultMap id="compamyAliasResult" type="CompanyAlias">
	</resultMap>

	<select id="getByCompanyId" resultMap="compamyAliasResult">
		select * from  company_alias where companyId = #{companyId}
	</select>

</mapper>