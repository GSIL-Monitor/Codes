alter table ios1 add index (artifactId,type,date);

alter table ios2 add index (artifactId,type,date);

alter table ios3 add index (artifactId,type,date);

alter table ios4 add index (artifactId,type,date);

alter table ios5 add index (artifactId,type,date);

alter table ios6 add index (artifactId,type,date);

alter table ios7 add index (artifactId,type,date);

alter table ios8 add index (artifactId,type,date);

alter table ios9 add index (artifactId,type,date);

alter table ios10 add index (artifactId,type,date);

alter table ios11 add index (artifactId,type,date);

alter table ios12 add index (artifactId,type,date);

alter table ios13 add index (artifactId,type,date);

alter table ios14 add index (artifactId,type,date);

alter table ios15 add index (artifactId,type,date);

alter table ios16 add index (artifactId,type,date);

alter table ios17 add index (artifactId,type,date);

alter table ios18 add index (artifactId,type,date);

alter table ios19 add index (artifactId,type,date);

alter table ios20 add index (artifactId,type,date);

alter table ios21 add index (artifactId,type,date);

alter table ios22 add index (artifactId,type,date);

alter table ios23 add index (artifactId,type,date);

alter table ios24 add index (artifactId,type,date);

alter table ios25 add index (artifactId,type,date);

alter table ios26 add index (artifactId,type,date);

alter table ios27 add index (artifactId,type,date);

alter table ios28 add index (artifactId,type,date);

alter table ios29 add index (artifactId,type,date);

alter table ios30 add index (artifactId,type,date);

alter table ios31 add index (artifactId,type,date);

alter table ios32 add index (artifactId,type,date);

alter table ios33 add index (artifactId,type,date);

alter table ios34 add index (artifactId,type,date);

alter table ios35 add index (artifactId,type,date);

alter table ios36 add index (artifactId,type,date);

alter table ios37 add index (artifactId,type,date);

alter table ios38 add index (artifactId,type,date);

alter table ios39 add index (artifactId,type,date);

alter table ios40 add index (artifactId,type,date);

alter table ios41 add index (artifactId,type,date);

alter table ios42 add index (artifactId,type,date);

alter table ios43 add index (artifactId,type,date);

alter table ios44 add index (artifactId,type,date);

alter table ios45 add index (artifactId,type,date);

alter table ios46 add index (artifactId,type,date);

alter table ios47 add index (artifactId,type,date);

alter table ios48 add index (artifactId,type,date);

alter table ios49 add index (artifactId,type,date);

alter table ios50 add index (artifactId,type,date);

alter table ios51 add index (artifactId,type,date);

alter table ios52 add index (artifactId,type,date);

alter table ios53 add index (artifactId,type,date);

alter table ios54 add index (artifactId,type,date);

alter table ios55 add index (artifactId,type,date);

alter table ios56 add index (artifactId,type,date);

alter table ios57 add index (artifactId,type,date);

alter table ios58 add index (artifactId,type,date);

alter table ios59 add index (artifactId,type,date);

alter table ios60 add index (artifactId,type,date);

alter table ios61 add index (artifactId,type,date);

alter table ios62 add index (artifactId,type,date);

alter table ios63 add index (artifactId,type,date);

alter table ios64 add index (artifactId,type,date);

alter table ios65 add index (artifactId,type,date);

alter table ios66 add index (artifactId,type,date);

alter table ios67 add index (artifactId,type,date);

alter table ios68 add index (artifactId,type,date);

alter table ios69 add index (artifactId,type,date);

alter table ios70 add index (artifactId,type,date);

alter table ios71 add index (artifactId,type,date);

alter table ios72 add index (artifactId,type,date);

alter table ios73 add index (artifactId,type,date);

alter table ios74 add index (artifactId,type,date);

alter table ios75 add index (artifactId,type,date);

alter table ios76 add index (artifactId,type,date);

alter table ios77 add index (artifactId,type,date);

alter table ios78 add index (artifactId,type,date);

alter table ios79 add index (artifactId,type,date);

alter table ios80 add index (artifactId,type,date);

alter table ios81 add index (artifactId,type,date);

alter table ios82 add index (artifactId,type,date);

alter table ios83 add index (artifactId,type,date);

alter table ios84 add index (artifactId,type,date);

alter table ios85 add index (artifactId,type,date);

alter table ios86 add index (artifactId,type,date);

alter table ios87 add index (artifactId,type,date);

alter table ios88 add index (artifactId,type,date);

alter table ios89 add index (artifactId,type,date);

alter table ios90 add index (artifactId,type,date);

alter table ios91 add index (artifactId,type,date);

alter table ios92 add index (artifactId,type,date);

alter table ios93 add index (artifactId,type,date);

alter table ios94 add index (artifactId,type,date);

alter table ios95 add index (artifactId,type,date);

alter table ios96 add index (artifactId,type,date);

alter table ios97 add index (artifactId,type,date);

alter table ios98 add index (artifactId,type,date);

alter table ios99 add index (artifactId,type,date);

alter table ios100 add index (artifactId,type,date);
